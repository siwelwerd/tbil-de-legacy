%!TEX root =../../../course-notes.tex
% ^ leave for LaTeXTools build functionality
\begin{applicationActivities}

\begin{observation}
In module C, we solved \term{constant coefficient linear ODEs}.
\vfill
Today we will observe that our existing techniques allow us to solve all \term{first order linear ODES}, i.e. ODEs of the form
\[a(x)y'+b(x)y+c(x)=0.\]
Such equations can always be rewritten (by rearranging and dividing by \(a(x)\) in \term{standard form}:
\[y'+P(x)y=Q(x).\]
\end{observation}

\begin{activity}{10}
\end{activity}

\end{applicationActivities}
